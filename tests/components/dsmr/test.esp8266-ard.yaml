substitutions:
  request_pin: GPIO15

packages:
  uart: !include ../../test_build_components/common/uart/esp8266-ard.yaml

<<: !include common.yaml

text_sensor:
  - platform: dsmr
    identification:
      name: "DSMR Identification"
    p1_version:
      name: "DSMR Version. OBIS: 1-3:0.2.8"

# The logger shares UART0 with this test's pins, as it does on real hardware
logger:
  baud_rate: 0
