substitutions:
  display_reset_pin: GPIO3
  interrupt_pin: GPIO15
  touch_reset_pin: GPIO16

packages:
  i2c: !include ../../test_build_components/common/i2c/esp8266-ard.yaml

<<: !include common.yaml

# The logger shares UART0 with this test's pins, as it does on real hardware
logger:
  baud_rate: 0
